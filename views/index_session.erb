<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>ダッシュボード - Keypod</title>
</head>
<body>
	<h2>Welcome <%= session_user.name %>!</h2>
	<a href="/logout">ログアウト</a>
	<a href="/workspace/create">ワークスペースの作成</a>
	<% @workspaces.each do |workspace| %>
	<div>
		<div>
			<h1><%= workspace.name %></h1>
		</div>
		<div>
			<small><%= workspace.description %></small>
		</div>
		<div>
			<a href="/workspace/<%= workspace.url %>">開く</a>
			<a href="/workspace/edit/<%= workspace.url %>">編集</a>
			<form action="/workspace/delete/<%= workspace.id %>" method="post">
				<input type="submit" value="削除">
			</form>
		</div>
	</div>
	<% end %>
</body>
</html>
